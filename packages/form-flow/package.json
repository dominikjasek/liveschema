{
  "name": "form-flow",
  "version": "0.0.1",
  "description": "Build a typed, branching multi-step form from any Standard Schema validators. Returns the ordered list of currently-reachable steps as the user fills in answers.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "@standard-schema/spec": "^1.0.0"
  }
}
